{{ form_start(form) }}
    <div class="form-actions top">
    </div>
    <div class="form-body">
        <div class="row">
            <div class="form-group col-sm-6">
                {{ form_label(form.codeAgence) }}
                {{ form_widget(form.codeAgence) }}
                <small style="display:none;" class="text-danger"> </small>
            </div>
            <div class="form-group col-sm-6">
                {{ form_label(form.libelle) }}
                {{ form_widget(form.libelle) }}
            </div>
        </div>
    </div>
    <div class="form-actions">
        <div class="btn-set pull-right">
        {# <a href="{{ path('sogema_mise_a_jour') }}" class="btn btn-danger">Mise à jour</a>  #}
            <button type="submit" class="btn green">Enregistrer</button>
            <a href="{{ path('app_agence_index') }}" class="btn btn-danger">Annuler</a>
        </div>
    </div>
{{ form_end(form) }}