{% extends 'base.html.twig' %}

{% block title %}EtatPretSystem{% endblock %}

{% block body %}
    <h1>EtatPretSystem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat_pret_system.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ etat_pret_system.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etat_pret_system_index') }}">back to list</a>

    <a href="{{ path('app_etat_pret_system_edit', {'id': etat_pret_system.id}) }}">edit</a>

    {{ include('etat_pret_system/_delete_form.html.twig') }}
{% endblock %}
