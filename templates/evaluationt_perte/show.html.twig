{% extends 'base.html.twig' %}

{% block title %}EvaluationtPerte{% endblock %}

{% block body %}
    <h1>EvaluationtPerte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluationt_perte.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ evaluationt_perte.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evaluationt_perte_index') }}">back to list</a>

    <a href="{{ path('app_evaluationt_perte_edit', {'id': evaluationt_perte.id}) }}">edit</a>

    {{ include('evaluationt_perte/_delete_form.html.twig') }}
{% endblock %}
