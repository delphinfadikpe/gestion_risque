{% extends 'base.html.twig' %}

{% block title %}Decisioncas{% endblock %}

{% block body %}
    <h1>Decisioncas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decisionca.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ decisionca.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_decisioncas_index') }}">back to list</a>

    <a href="{{ path('app_decisioncas_edit', {'id': decisionca.id}) }}">edit</a>

    {{ include('decisioncas/_delete_form.html.twig') }}
{% endblock %}
