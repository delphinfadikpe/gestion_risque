<form method="post" action="{{ path('app_bale_niveau2_delete', {'id': bale_niveau2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bale_niveau2.id) }}">
    <button class="btn">Delete</button>
</form>
