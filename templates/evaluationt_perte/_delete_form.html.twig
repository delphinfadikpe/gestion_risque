<form method="post" action="{{ path('app_evaluationt_perte_delete', {'id': evaluationt_perte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluationt_perte.id) }}">
    <button class="btn">Delete</button>
</form>
