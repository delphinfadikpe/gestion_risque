{% extends 'base.html.twig' %}

{% block title %}ClassificationPerte{% endblock %}

{% block body %}
    <h1>ClassificationPerte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classification_perte.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ classification_perte.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classification_perte_index') }}">back to list</a>

    <a href="{{ path('app_classification_perte_edit', {'id': classification_perte.id}) }}">edit</a>

    {{ include('classification_perte/_delete_form.html.twig') }}
{% endblock %}
